# Общее ядро конвертации Markdown <-> Word: используется и GUI (requirements.txt),
# и MCP-сервером (requirements-mcp.txt), поэтому вынесено в отдельный файл.
python-docx==1.1.2
Pillow==12.0.0
markdown-it-py==4.0.0
mdit-py-plugins==0.5.0
linkify-it-py==2.0.3
