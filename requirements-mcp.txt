# Зависимости MCP-сервера (mdtoword/mcp_server.py).
# Ядро конвертации подключается из requirements-core.txt, а не requirements.txt:
# так серверный SDK mcp не тянет за собой PyQt6, который нужен GUI,
# а headless-серверу — нет.
-r requirements-core.txt
mcp==1.28.1
